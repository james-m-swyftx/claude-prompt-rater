---
description: Turn off the prompt-rater status line (removes it from your settings.json)
allowed-tools: Bash(bash:*)
---

Disable the prompt-rater status line by running this exact command with the Bash tool:

```
bash "${CLAUDE_PLUGIN_ROOT}/scripts/disable.sh"
```

Then report the result to the user. The change takes effect on the next render or restart.
